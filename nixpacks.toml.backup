[phases.setup]
nixPkgs = ["python311", "uvicorn"]
[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install PyJWT==2.8.0",
    "pip install -r requirements.txt"
]
[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port \"
